# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: windows-agent-sonal

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'cd C:\agents\vsts-agent-win-x64-5.276.0\_work\4\s'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'pwd'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'terraform init'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'terraform init -reconfigure'
    backendServiceArm: 'asr'
    backendAzureRmStorageAccountName: 'sonalfortesting'
    backendAzureRmContainerName: 'backendtest'
    backendAzureRmKey: 'rg.test'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'asr'
    backendAzureRmStorageAccountName: 'sonalfortesting'
    backendAzureRmContainerName: 'backendtest'
    backendAzureRmKey: 'rg.test'